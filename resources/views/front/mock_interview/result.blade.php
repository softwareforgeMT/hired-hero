here: @extends('front.layouts.app')
@section('title') Interview Results @endsection
@section('css')
<style>
  .question-answer{
    border-bottom: 1px solid white;
  }
</style>
@endsection
@section('content')
    <div class="page__content">
        <section class="section">
            <div class="container">
                
                 <!-- Button to start another mock interview -->
                <div class="mt-4 d-flex justify-content-between">
                    <h1>Interview Results</h1>
                    <a href="{{ route('mock.job-details.create') }}" class="btn btn-primary d-flex align-items-center">Start Another Mock Interview</a>
                </div>

                @if(isset($error))
                    <div class="alert alert-danger">{{ $error }}</div>
                @else
                <div class="alert alert-info mt-4">
                    <strong>Note:</strong> These evaluation results are generated by AI and may not be 100% accurate. Please use them as a guide rather than a definitive assessment.
                </div>

                    <div class="row">
                        <div class="col-md-8">
                            <div class="card">
                                <div class="card-body">
                                    <h2 class="h5 font-weight-bold">Overall Evaluation</h2>
                                    <p>{!! nl2br(htmlspecialchars($result)) !!}</p>
                                </div>
                            </div>
                        </div>
                        <div class="col-md-4">
                            <div class="card">
                                <div class="card-body">
                                    @foreach($interviewResultsData as $key => $data)
                                        <div class="question-answer p-3">
                                            <h2 class="h5 font-weight-bold">Question {{$key+1}}: {!! htmlspecialchars($data['question_text']) !!}</h2>
                                            <p>Answer: {!! htmlspecialchars($data['transcription']) !!}</p>
                                            <audio controls class="w-100 mt-3">
                                                <source src="{{ asset('mock/recordings/' . $data['audio_filename']) }}" type="audio/webm">
                                                Your browser does not support the audio element.
                                            </audio>
                                        </div>
                                    @endforeach
                                </div>
                            </div>
                        </div>
                    </div>
                @endif
            </div>
        </section>
    </div>
@endsection
@section('script')
@endsection